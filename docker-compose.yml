# docker-compose.yml
version: "2"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8765:80"
    container_name: multi_blanco
    expose:
      - "80"
networks:
  default:
    external:
      name: nginx-proxy_default
